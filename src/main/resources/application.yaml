spring:
 application:
  name: shopping-cart
 datasource:
   url: jdbc:mysql://localhost:3306/shoppingcart
   username: root
   password: NewPassword123!
 flyway:
   baseline-on-migrate: true
 jpa:
  hibernate:
    ddl-auto: update
  show-sql: true

